[package]
name = "bingo-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
pdf-writer = "0.14"
image = { version = "0.25", default-features = false, features = [
  "jpeg",
  "png",
] }
